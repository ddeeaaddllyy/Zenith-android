package com.ddeeaaddllyy.zenith.ui.theme

import androidx.compose.ui.graphics.Color

// --- Chocolatte (default) ---
val SoftDove = Color(0xFFC0BAB3)
val SpicedHotChocolate = Color(0xFF52423D)
val MoonRock = Color(0xFF887D77)
val DarkSienna = Color(0xFF391214)
val BlackRaspberry = Color(0xFF160F0C)

val SurfaceElevated = Color(0xFF3D302B)
val SurfaceContainer = Color(0xFF241A17)

// --- Early Winter ---
val Concrete = Color(0xFF525871)
val WarmSteel = Color(0xFF857C91)
val Rosewood = Color(0xFFCD9FA0)
val PeachesAndCream = Color(0xFFF2C1A3)
val Dawn = Color(0xFFF8CCAA)

// --- Blooming Period ---
val WarmFog = Color(0xFFDDD3C9)
val BerryGood = Color(0xFFECC4C3)
val UsuKoubaiBlossom = Color(0xFFB97D7B)
val MeadowMauve = Color(0xFF928E5E)
val SoldierGreen = Color(0xFF575527)

// --- Obsidian Tide (original) ---
val ObsidianBackground = Color(0xFF0E1A1F)
val ObsidianSurface = Color(0xFF1B2E33)
val ObsidianSeafoam = Color(0xFF7FC6BA)
val ObsidianSteel = Color(0xFF5C7D82)
val ObsidianIce = Color(0xFFDCEDEB)

// A dedicated light palette for the calendar dialog — deliberately breaks from the dark theme.
val CalendarWhite = Color(0xFFFFFFFF)
val CalendarSurfaceVariant = Color(0xFFF1ECE8)

// Fixed across every theme — used for the destructive swipe-to-delete background.
val AlertRed = Color(0xFFB3261E)
val OnAlertRed = Color(0xFFFFFFFF)

// Fixed across every theme — the "not signed in" notice reads as a quiet, dark red, not an alarm.
val NoticeDarkRed = Color(0xFF4A1616)
val OnNoticeDarkRed = Color(0xFFE8C9C9)

// Fixed across every theme — the registration success glow always reads as green.
val SuccessGreen = Color(0xFF3DDC84)
